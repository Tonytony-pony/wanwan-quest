# わんわんクエスト 開発メモ

この先の作業を再開するための引き継ぎ資料。
遊び方や公開手順は [README.md](README.md) を参照。ここには**設計の理由**と
**ハマりどころ**を残す。

最終更新: 2026-09-07 / コミット 20本

---

## 1. 全体像

たしざんを学びながら犬を育てる、レトロRPG風の Web アプリ。
`index.html` 1ファイルで完結（フレームワーク・ビルドなし）。

```
桜 算数アプリ/
├ index.html            アプリ本体（約72KB / 1400行）
├ README.md             遊び方・公開手順・調整方法
├ 開発メモ.md            このファイル
├ tools_make_dog_png.py 画像変換（背景除去 → アプリ用PNG）
├ tools_draw_dog.py     旧ドット絵の生成（現在は未使用。参考として保持）
├ .nojekyll             GitHub Pages 用
├ img/                  アプリが読む PNG（37枚 / 約4MB）
│   ├ <犬種>_lv1.png _lv3.png _lv5.png _sad.png   9犬種 × 4 = 36枚
│   ├ paw.png                                     全犬種で共有
│   └ house.png bowl.png ball.png medal.png       アイテム4枚
└ イラスト犬/            Gemini で作った元画像（JPG）
    ├ <犬種>/01〜04.jpg  9犬種
    ├ items/*.jpg
    └ paw.jpg
```

外部読み込みは **Google Fonts（DotGothic16）1件のみ**。
オフラインでも `MS Gothic` 等にフォールバックして動く。

---

## 2. 画面と遷移

```
タイトル ──→ ワンワンハウス ──→ おさんぽ ──→ クイズ ──→ リザルト
（きみの名前）  （犬えらび）      （コース）    （10問）   ├→ おさらい
                                                      └→ ゲームオーバー
```

`var SCREENS = ['title','select','map','quiz','result','gameover']`
`show(name)` で1つだけ表示する方式。

### ワンワンハウス（`screen-select`）
上部に **position:sticky のタブ5つ**。

| タブ | 中身 | パネルid |
|---|---|---|
| あそぶ | 犬カード9枚のグリッド | `sel-list-win` |
| スタンプ | カレンダー＋れんぞく日数＋ボーナス表 | `pane-stamp` |
| ずかん | たからもの25種のコレクション | `pane-zukan` |
| アイテム | アイテム4つの一覧（レベル・効果・所持犬） | `pane-items` |
| あそびかた | ルール文＋「さいしょから やりなおす」 | `pane-how` |

`showPane(which)` が `play / stamp / zukan / items / how / name / reset` を切り替える。

タブ5つを1行に収めるため、文字サイズは `clamp(10px, 3.05vw, 13px)` ＋ `white-space:nowrap`。
固定サイズだと「あそびかた」が2行に折り返す。
`name` は新しい犬の命名パネル、`reset` は初期化の確認パネル。

---

## 3. ゲームの数値（すべて `index.html` の「0. せってい」）

```javascript
QUESTIONS_PER_RUN = 10    // 1コースの問題数
AFFECTION_MAX     = 100   // レベルアップに必要な ♥
HEART_START       = 3     // おさんぽ開始の ♥
HEART_UP          = 1     // 正解で増える ♥
HEART_DOWN        = 3     // 不正解で割れる ♥
WEAK_RATE         = 0.4   // にがて問題が出る確率
HOUSE_HEART = 1 / BALL_STREAK = 3 / MEDAL_BONUS = 5
```

**なつき度＝これまでに集めた ♥ の合計**。おさんぽ中は ♥ だけが動き、
終了時に残った ♥ がそのまま なつき度に加算される。

| 結果 | 残る ♥ | なつき度 |
|---|---|---|
| 全問正解 | 13 | +13 |
| 1ミス | 9 | +9 |
| 2ミス | 5 | +5 |
| 3ミス | 1 | +1 |
| ♥ が足りず終了 | 0 | +0（既存のなつき度は減らない） |

♥ が `HEART_DOWN` 未満のときに間違えると「いぬが いなくなる」。
つまり**4ミスは構造的に不可能**、3ミスまで生存できる。

### まいにちスタンプ（れんぞく日数）

おさんぽを1回終えると、その日のスタンプが1つ押される。
**成績に関係なくもらえる**（ゲームオーバーでももらえる）。1日1個まで。

| 定数 | 値 | 意味 |
|---|---|---|
| `STREAK_BONUS` | 3日→+3 / 5日→+5 / 7日→+10 | れんぞく日数のごほうび（なつき度） |
| `STREAK_LOOP` | 7 | 7日を超えたら何日ごとにボーナスか |
| `STREAK_LOOP_AFF` | 10 | そのときのなつき度 |

実装場所

| やること | 関数 |
|---|---|
| その日のキーを作る | `dkey(d)` |
| 日数の差を出す | `dayDiff(a, b)` |
| 今なん日つづいているか | `liveStreak()` |
| 今日やらないと切れるか | `streakAtRisk()` |
| スタンプを押す | `stampToday()` |
| カレンダーを描く | `renderStamp()` |

**`dkey()` は必ずローカル時間で組み立てること。** `toISOString()` は UTC なので、
日本の夜21時以降が前日扱いになり、連続日数が壊れる。

`stampToday()` は `finishRun()` と `gameOver()` の両方から呼ぶ。
`finishRun()` ではボーナスを `run.gained` に足してからレベルアップ判定に入るので、
**足す順番を変えないこと**（先にレベルアップ計算をするとボーナス分が繰り越されない）。

### たからもの ずかん

おさんぽを最後まで終えると、犬が1こ拾って帰る。**全問正解ならもう1こ**。
**ゲームオーバーでは拾わない**（歩き切れていないため）。スタンプとは違い、
これは「完走のごほうび」として区別している。

| レア度 | 確率 | 種類数（1コースあたり） |
|---|---|---|
| ふつう | 57% | 3 |
| めずらしい | 30% | 2 |
| とても めずらしい | 10% | 1 |
| でんせつ（🌈 にじの かけら） | 3% | 全コース共通で1 |

**コースごとに出るものが違う**のが肝。難しいコースを解放したあとも
やさしいコースに戻る理由になる（小1が毎日2けた＋2けたを強制されない）。

全24種＋でんせつ1種＝**25種**。コンプリート報酬は
1コース6こ全部で なつき度＋10、25こ全部で ＋50（`save.zbonus` で1回だけ）。

| やること | 関数 |
|---|---|
| 抽選 | `drawTreasure(courseId)` |
| 拾った記録 | `pickUp(t)` |
| コース別の絞り込み | `treasuresOf()` / `treasuresByCourse()` |
| コンプ判定 | `courseZukanDone()` / `allZukanDone()` |
| 図鑑を描く | `renderZukan()` |

セーブは `found: { id: 個数 }` と `zbonus: { コースid / 'all': true }`。
犬ではなく子どもに紐づくので `dogs` の外。

**絵文字は Emoji 12.0（2019）までに限ること。** 🪶🫐🪨🪵 は 2020-2021 の追加で、
少し古い端末では豆腐（□）になる。canvas の `measureText` で
`\u{10FFFD}` と同じ幅なら未対応、と判定できる。
この方法で全25種を検査して、🪶→🐿️、🫐→🍇、🪨→☁️ に差し替えた。

### ミニゲーム「ボールキャッチ」

おさんぽ画面の金色の窓から入る独立した画面（`screen-catch`）。
落ちてくる3つのボールから答えをタップ。60秒。

| 定数 | 値 | 意味 |
|---|---|---|
| `CATCH_SEC` | 60 | 制限時間（秒） |
| `CATCH_FALL0` | 5500 | 開始時の落ちきる時間(ms) |
| `CATCH_FALL_MIN` | 3000 | 最速時 |
| `CATCH_FALL_DEC` | 70 | 1キャッチごとに短くなる分 |
| `CATCH_STEP` | 6 | 何キャッチで1段階むずかしくするか |
| `CATCH_AFF_PER` / `CATCH_AFF_MAX` | 3 / 6 | 何キャッチでなつき度1／1回の上限 |

**♥は減らない。スタンプもたからものも出ない。** おさんぽを主役のまま
保つための線引き。報酬は なつき度 最大+6（おさんぽの13より意図的に低い）と
`save.catchBest` のハイスコア。

難易度は `catchCourse(score)` が6キャッチごとに1段階上げるが、
**まだ解放していないコースは出さない**。

#### ハマった点2つ（どちらも再発しやすい）

**1. フレーム差分でゲーム時間を進めてはいけない。**
最初 `dt = min(50, ts - last)` を積算していたが、ブラウザが描画を
間引く状況（検証用ペインでは rAF が毎秒2回しか来なかった）だと
ゲーム全体がスローになり、**残り時間まで止まった**。
今は残り時間を `cg.endAt - performance.now()` の壁時計、
ボール位置を `y0 + spd * (now - t0)` の時間の関数にしている。
フレームが落ちても速さは変わらない。

**2. 速度を px/ms で決めると画面の高さで難易度が変わる。**
落下エリアはスマホ549px・タブレット761px。同じ px/ms だと
タブレットでは落ちきるのに10秒かかりゲームにならない。
**「落ちきる秒数」で決めて px/ms は毎回そこから逆算する。**
これで全端末 5.5秒→3.0秒に揃う。

**難易度の調整はこの3つだけ触ればよい。** 落下時間はプレイのテンポではなく
「間に合わなかったら失敗」の締め切りなので、速く解ける子ほど
たくさん取れる。遅くしても取れる数は大きくは減らない。
初期版は 4.0秒→2.0秒だったが「むずかしい」との声で緩めた（2026-09-08）。

一時停止は `visibilitychange` で行い、再開時に
`cg.endAt` と全ボールの `t0` を停止時間ぶんずらす。
`#app.fit` はもんだい画面と同じくこの画面でも付ける。

### たからものゲットの演出

**横長の通知バーではなく、画面中央のモーダルで出す。** 上部に流すと
「もらった感」が薄く、レアを引いたときの嬉しさが出ない。

`showTreasurePops(list)` がキューで1つずつ表示。2こ拾ったら
ボタンが「つぎの おたから ▶」→「やったー！」に変わる。
結果画面には `ひろってきた 🌼🐌` の1行だけ残す（記録は図鑑が持つ）。

レア度で演出を段階的に強くしている。

| クラス | 見た目 | きらきら | 音 |
|---|---|---|---|
| `r0` | ピンクのカード | 10コ | 2音 |
| `r1` | レア度バッジが青 | 14コ | 2音（高め） |
| `r2` | 背後で光線が回転（金） | 20コ | 3音 |
| `r3` | 光線が速く回る＋枠が虹色に変化 | 28コ | 6音のファンファーレ |

`.first`（初回発見）は上に重ねてカードを金色にする。
`.r3` は `animation` で `border-color` を動かすので `.first` の
静的な border より優先される（意図どおり）。

### あそびかたはカテゴリ分け

1つづきの文章だと読みにくいので `RULE_SECTIONS` 配列から組み立てる。
数字は `HEART_START` などの定数から作るので、**バランスを変えても文章がずれない**。

- タイトル画面は `ruleShortHTML()`（おさんぽの4行＋タブへの案内）
- 「あそびかた」タブは `ruleHTML()`（5カテゴリ全部）

`li.key` を付けた行はピンク地で強調する（「まちがえると♥3こ われる」など）。
中に `<div>` を入れるので、この2つの入れ物は `<p>` ではなく `<div>`。

### 見た目が変わるレベル

| レベル | よび名 | 絵 |
|---|---|---|
| 1〜2 | こいぬ | `_lv1` |
| 3〜4 | わんこ | `_lv3` |
| 5〜7 | たくましい 犬 | `_lv5` |
| 8〜 | でんせつの 犬 | `_lv5`（名前だけ変化） |

1日1コースで ♥ が9〜13たまるので、レベルアップは **8〜11日に1回**。

### ごほうびアイテム（犬ごと）

| Lv | アイテム | 効果 | 実装箇所 |
|---|---|---|---|
| 10 | いぬ小屋 | ♥スタート 3→4 | `startRun` |
| 15 | ごはんの おさら | 1回だけ ♥ が割れない | `answer` の `run.guard` |
| 20 | ボール | 3問連続正解ごとに ♥+1 | `answer` の `run.streak` |
| 25 | きんの メダル | 終了時 なつき度+5 | `finishRun` の `run.medalBonus` |

**所持はレベルから導出**（`hasItem()`）。セーブには保存しない。
4つそろうと全問正解の日が なつき度22 になり、約1.7倍のペース。

---

## 4. セーブデータ

```javascript
localStorage['wanwanquest_save_v1'] = {
  name:'さくら',          // 飼い主の名前
  current:'shiba',        // いま選んでいる犬種
  dogs:{                  // 犬ごと（何匹でも育てられる）
    shiba:{ dogName:'もも', level:26, affection:40 }
  },
  cleared:{garden:true},  // ここから下は全犬種で共有
  mistakes:{'garden|2+3':2},
  totalOk:200, totalNg:20,
  sound:true, bgm:true
}
```

**犬ごと**: なまえ / レベル / なつき度
**共有**: コース解放・にがて問題（＝子どもの学習の進み具合なので犬を変えても引き継ぐ）

`pet()` が現在の犬のオブジェクトを返す。`loadSave()` に**旧形式からの移行**が
入っている（1匹だけの古いセーブ → ビーグルへ）。

---

### スタンプ関連のセーブ項目

犬ではなく**子ども**に紐づくので、`dogs` の外（共有側）に置いている。

```
days    : { 'YYYY-MM-DD': その日あそんだ回数 }
streak  : いま何日つづいているか
best    : これまでの最長記録
lastDay : 最後にあそんだ日
```

`save.streak` は最後にスタンプを押した時点の値なので、**表示には必ず
`liveStreak()` を使う**（何日も空いていると `save.streak` は古い値のまま残る）。

---

## 5. 画像パイプライン（ここが一番ハマった）

### 大原則：Gemini に**グリーンバック**を描かせる

Gemini は**本当の透過を出力できない**。`transparent background` と指示すると
「透過を表す市松模様」を**絵として描いてしまう**。ダウンロードボタンで保存しても
市松は画像の中身なので消えない。

そこで **背景に単色グリーン（#00FF00）を描かせて抜く**。
これに切り替えてから一度も失敗していない（変換ログの「かたまり=1」が目印）。

```bash
python tools_make_dog_png.py            # ぜんぶ
python tools_make_dog_png.py shiba      # 1犬種だけ
python tools_make_dog_png.py items      # アイテムだけ
python tools_make_dog_png.py paw        # あしあとだけ
```

### スクリプトがやること

1. 四隅の色を見て**単色背景か市松模様か**を自動判定（`key_color()`）
2. 単色 → 色距離で抜く＋緑のにじみを落とす（`despill()`）
3. 市松 → 格子を検出して白マスを除去（`checker_mask()`。後述）
4. 前景を一度やせさせて塊を選び、太らせて戻す（細い接続を切る）
5. 512×512 に配置。**犬は下端そろえ**（足の高さを統一してレベルアップで
   絵が飛ばないように）、**あしあとは中央**、**アイテムは余白なし**
6. 高さを正規化: lv1=360 / lv3=400 / lv5=440 / sad=400

透過PNGを入力した場合は 1〜4 を全部飛ばしてそのまま使う。

### 市松模様まわりの苦労（過去の資産用。今後は不要）

白い犬（フレンチブルドッグ）で**体の白(253)と市松の白マス(255)が区別できず、
体まで消える**問題が起きた。解決策:

- 背景判定を「無彩色のグレー(178〜248)」に限定し、純白は残す
- 市松の白マスは**四方がグレーマス**という性質で犬の白と区別
- 犬の輪郭がかかったマスは、マス全体ではなく**純白の画素だけ**を除去

### 変換で直せない失敗（元画像で気をつける）

- **枠の外で切れている** → 切れたまま。とくにチワワの耳、王冠、マント
- **しっぽや飾りが大きすぎる** → 高さを揃える都合で犬本体が小さくなる

上下位置のズレは自動で直るので気にしなくてよい。

---

## 6. Gemini プロンプトの型（他の犬種を足すとき）

### 添付する画像

| 作る絵 | 添付 |
|---|---|
| 01 | `img/beagle_lv1.png`（垂れ耳系）または `img/shiba_lv1.png`（立ち耳系） |
| 02 | ①その犬種の01 ＋ ②`img/beagle_lv3.png` か `shiba_lv3.png` |
| 03 | ①その犬種の01 ＋ ②`..._lv5.png` |
| 04 | ①その犬種の01 ＋ ②`..._sad.png` |

**必ず「Image 1 は STYLE reference のみ。耳・色・柄はコピーするな」と書く。**
これを省くと参照画像の犬種に引きずられる（ゴールデンがビーグル化した）。

### 効いた指示（試行錯誤の結果）

```text
EYES: 目は「犬の横幅の1/8」＝「鼻とほぼ同じ大きさ」。
      大きい目は怖くなる。実測値は 11〜13%。
      丸い濃色の円＋左上に白ハイライト1つ。まぶた・半月形は禁止。
      白い丸に黒い点はダメ（ビーズ目になる）。
MOUTH: 鼻のすぐ下に、鼻の2倍の幅で大きく笑わせる。
MUZZLE: 短く丸く。縦長のクサビにすると顔がとがって怖くなる。
BODY: 体の幅は頭の幅の 2/3（実測 66〜95%、ビーグルの66%が一番かわいい）。
LEGS: 全体を10として 頭4 / 体4 / 脚2。「コーギーのように短く」。
      大型犬（ゴールデン・ハスキー）は必ず脚が伸びるので毎回書く。
TAIL: 頭より小さく、耳より高く上げない。ポメ・柴・ハスキーで暴れる。
```

### ポーズ4種

- **01 こいぬ**: 座り・正面・赤ちゃん体型
- **02 わんこ**: 四足で立つ・正面・少し大きい（**01との違いは「立っている」こと**）
- **03 でんせつ**: 王冠＋ローズピンクのマント・正面
- **04 そっぽ**: 後ろ姿・顔は一切見せない・しっぽは下げる

背景は毎回 `solid GREEN (#00FF00)` を明示。

---

## 7. 実装上の注意（再発しやすいもの）

**画像の遅延読み込みで犬が消える。**
そっぽを向いた画像は不正解になるまで読み込まれず、表示の瞬間に
`height:auto` で高さ0になる。対策として `preloadDog()` でおさんぽ開始時に
4枚を先読みし、`.dog` に `aspect-ratio:1/1` で場所を確保している。
**新しい画像を増やすときは preload に足すこと。**

**`window.confirm()` は環境によって抑制される。**
黙って false が返り、ボタンが効かなくなる。アプリ内の確認パネルに置き換え済み。
今後もネイティブダイアログは使わない。

**白い犬はピンク背景に沈む。**
`.dog` に淡い輪郭影を足して解決（`drop-shadow(0 0 2px ...)`）。

**小屋の位置は犬種で見え方が変わる。**
犬の表示幅が 265px（チワワ）〜437px（ダックス）と幅があるため、
`left:16px` に固定して**どの犬でも77%以上見える**ようにしてある。

---

### もんだい画面は「高さ追従」レイアウト

**選択肢3つは横1列**（`.choices{display:flex}`）。縦に並べると247px使い、
812pxの画面でもスクロールが出ていた。

さらに、回答するとメッセージが3行に伸びて `.win` が140→188pxになり、
「つぎへ」が画面外に押し出される。これを固定値で詰めるのは端末ごとに破綻するので、
**犬の絵が余った/足りない高さを吸収する**構造にした。

```
#app.fit          height:100dvh    ← これが無いと flex が縮まない
  #screen-quiz    flex column
    .dog-stage    flex:1  min-height:96px   ← ここが伸縮する
```

`#app` は通常 `min-height:100vh` なので、内容が多いと**コンテナ自体が伸びてしまい
flex-shrink が働かない**。`show('quiz')` のときだけ `fit` クラスを付けて
高さを確定させている。他の画面（犬リスト、カレンダー）は今まで通りスクロールする。

回答後は `#screen-quiz.answered` が付き、選択肢を小さくし
「おさんぽへ もどる」を隠す（「つぎへ」だけにする）。
`answered` の付け外しは4箇所 — 出題時・通常回答・ごはんのおさら・おさらい。
**新しく「つぎへ」を出す分岐を足したら、ここも足すこと。**

検証済み: 320×568 / 360×640 / 375×667 / 375×812 / 414×896 / 768×1024
すべて未回答・回答後ともスクロールなし。

---

## 8. 音

音声ファイルは使わず **Web Audio API のオシレーター**で鳴らす。

- 効果音: `tone()` と `sfxOk/sfxNg/sfxLevel/sfxOver/sfxTap`
- BGM: ハ長調8小節のループ（`BGM_MELODY` / `BGM_BASS`）。
  メロディ=矩形波、ベース=三角波。`setInterval` で先読みスケジュール
- **♪（こうかおん）と ♬（BGM）は別々に切り替え**
- ブラウザの制限で**最初のタップまで鳴らない**。タイトルの開始ボタンで `bgmStart()`
- タブが非表示のあいだは `bgmStop()`

---

## 9. 犬種（9種）

| 犬種 | id | 特徴 |
|---|---|---|
| ビーグル | `beagle` | 最初の1匹。全犬種のスタイル基準 |
| しばいぬ | `shiba` | 立ち耳系の基準 |
| ミニチュアダックス | `dachshund` | ブラック＆タン。02のみ体を斜めに |
| ポメラニアン | `pomeranian` | もふもふ |
| チワワ | `chihuahua` | 巨大な耳。枠から切れやすい |
| フレンチブルドッグ | `frenchbulldog` | 白黒パイド |
| ビションフリーゼ | `bichon` | 真っ白。輪郭影が必須 |
| ゴールデンレトリバー | `golden` | ビーグル化しやすい |
| ハスキー | `husky` | 青い目。オオカミ顔になりやすい |

**コーギーは見送り**（マズルの表現が難航したため）。
足す場合は `イラスト犬/corgi/` に4枚置いて `BREEDS` に1行追加するだけ。

---

## 10. 残っている案・未実装

- ボールキャッチ（ミニゲーム）— 次の実装候補
- ひきざんコース
- たからものを25種より増やす場合、`TREASURES` に足すだけでよい
  （図鑑・抽選・コンプ判定はすべて配列から自動で組み立てている）
- コーギーの追加（10犬種目）
- 「おさらい」は結果画面につき1回だけ。回数制限を変えるなら `run.reviewDone`
- レベル25より上のごほうび（現在は打ち止め）
- ~~GitHub Pages への公開~~ → 公開済み
  https://tonytony-pony.github.io/wanwan-quest/
  更新は `git push` するだけで1〜2分後に反映される

---

## 11. 決定の記録（なぜそうなっているか）

**なつき度と♥を1つの単位にした。** 当初は「なつき度100 / ごきげん100」の
2本ゲージで、どちらも不正解で減るため関係が分かりにくかった。
「おさんぽ中は♥だけ動き、終了時にそのまま なつき度になる」方式に変更。
これで**間違えた場所で最終結果が変わる**問題（1問目のミスは軽く、
10問目は重い）も解消した。

**にがて問題とコース解放は犬に紐づけない。** これは犬の成長ではなく
子どもの学習の進み具合なので、犬を変えるたびに1桁の足し算からやり直すのは不自然。

**おさらいでは減点しない。** 失うものがないので何度でも挑戦できる。

**スタンプは成績に関係なくもらえる。** レベルアップは8〜11日に1回しか起きないので、
その間の「今日は何も起きなかった」を埋めるのが目的。
1問まちがえると♥3個という厳しさは残したまま、
**毎日戻ってくる理由だけ**を確保している。ゲームオーバー画面にも出すのは、
失敗した日にこそ離脱しやすいため。

**カレンダー用のスタンプは専用画像を作った。** `paw.png` は足あと2つ＋薄い茶色で、
30px に縮めると潰れて読めなかった。`stamp.png` は足あと1つ・濃いピンク・余白なし。
正解するとにがて度が1つ減り、リストから卒業できる。

**ドット絵は全廃した。** Gemini のイラストに統一した時点で `ART` が
32KBの死にコードになったため削除（83KB→51KB）。生成スクリプトだけ残してある。
